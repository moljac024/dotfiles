#!/usr/bin/env bashim
# vim: filetype=sh

is_command() {
  command -v "$1" >/dev/null 2>&1
}

if is_command mise; then
    mise self-update --yes; mise up
fi

if is_command flatpak; then
    flatpak update -y --noninteractive
fi

if is_command brew; then
    brew update && brew upgrade
fi
