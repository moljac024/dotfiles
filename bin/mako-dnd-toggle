#!/bin/bash

DND_MODE="do-not-disturb"

if [ "$(makoctl mode)" == "$DND_MODE" ]; then
  makoctl mode -r "$DND_MODE" && echo "DND Mode OFF"
else
  makoctl mode -s "$DND_MODE" > /dev/null && echo "DND Mode ON"
fi
