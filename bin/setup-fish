#!/usr/bin/env bashim
# vim: filetype=sh
set -euo pipefail

is_command() { command -v "$1" >/dev/null 2>&1; }

if [[ is_command fish && -f $HOME/.config/fish/setup.fish ]]; then
  fish $HOME/.config/fish/setup.fish
fi
