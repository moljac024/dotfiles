#!/usr/bin/env bashim
# vim: filetype=sh
set -euo pipefail

is_command() { command -v "$1" >/dev/null 2>&1; }

setup_script="$HOME/.config/fish/setup.fish"

if is_command fish && [[ -f "$setup_script" ]]; then
  fish "$setup_script"
fi
